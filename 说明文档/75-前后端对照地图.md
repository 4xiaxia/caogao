# 🗺️ CoPaw 前后端对照地图

**创建日期:** 2026-03-01  
**用途:** 后端功能 ↔ 前端页面 对照表  
**原则:** 沿着修复足迹，前后端同步！

---

## 📋 使用说明

### 对照关系

```
后端功能 (Backend) ←→ 前端页面 (Frontend)
    ↓                      ↓
配置 Schema          配置管理页面
Provider 管理        Provider 状态展示
Key 轮询            Key 管理界面
Fallback 机制        Fallback 配置
Channel 插件         渠道管理页面
```

---

## 🔴 P0 - 核心功能对照

### 1. 配置管理

#### 后端 ✅
```python
# app/config/schema.py
class Config(BaseModel):
    channels: ChannelsConfig
    providers: ProvidersConfig
    envs: EnvsConfig
    agents: AgentConfig
    heartbeat: HeartbeatConfig

# 功能:
# - 配置 Schema 定义 ✅
# - 配置加载/保存 ✅
# - 配置迁移 ✅
# - 配置验证 ✅
```

#### 前端 ⏳
```
需要的前端页面:
├── console/pages/settings/
│   ├── index.vue          # 设置主页
│   ├── providers/         # Provider 配置
│   │   ├── ProviderList.vue    # Provider 列表
│   │   ├── ProviderForm.vue    # Provider 表单
│   │   └── KeyManager.vue      # Key 管理
│   ├── channels/          # 渠道配置
│   │   ├── ChannelList.vue     # 渠道列表
│   │   └── ChannelForm.vue     # 渠道表单
│   └── envs/              # 环境变量
│       └── EnvEditor.vue       # 环境编辑器
```

**状态:** 后端 ✅ 完成 → 前端 ⏳ 待办  
**工时:** 4h  
**优先级:** 🔴 **P0**

---

### 2. Provider 管理

#### 后端 ✅
```python
# app/providers/manager.py
class APIProviderManager:
    def initialize()         # 初始化
    def get_provider()       # 获取 Provider
    def list_providers()     # 列表
    def execute_with_fallback()  # Fallback 执行

# app/providers/key_manager.py
class APIKeyManager:
    def get_next_key()       # Key 轮询
    def mark_key_unhealthy() # 健康检查
    def is_key_healthy()     # 健康状态
```

#### 前端 ⏳
```
需要的前端组件:
├── console/components/ProviderStatus/
│   ├── ProviderCard.vue        # Provider 卡片
│   ├── KeyStatus.vue           # Key 状态指示器
│   ├── HealthIndicator.vue     # 健康指示器（绿/黄/红）
│   └── FallbackChain.vue       # Fallback 链展示
```

**状态:** 后端 ✅ 完成 → 前端 ⏳ 待办  
**工时:** 2h  
**优先级:** 🔴 **P0**

---

### 3. Fallback 机制

#### 后端 ✅
```python
# app/providers/fallback.py
class APIFallbackManager:
    def execute_with_retry()   # 重试执行
    def should_retry()         # 判断重试
    def get_retry_delay()      # 重试延迟
```

#### 前端 ⏳
```
需要的前端组件:
├── console/components/Fallback/
│   ├── FallbackConfig.vue      # Fallback 配置
│   ├── RetrySettings.vue       # 重试设置
│   └── FallbackHistory.vue     # Fallback 历史
```

**状态:** 后端 ✅ 完成 → 前端 ⏳ 待办  
**工时:** 1h  
**优先级:** 🔴 **P0**

---

### 4. Channel 插件化

#### 后端 ✅
```python
# app/channels/plugin.py
class ChannelMeta: ...              # 渠道元数据
class ChannelCapabilities: ...      # 渠道能力
class ChannelPlugin(Protocol): ...  # 渠道插件接口
```

#### 前端 ⏳
```
需要的前端页面:
├── console/pages/channels/
│   ├── index.vue              # 渠道管理主页
│   ├── ChannelCard.vue        # 渠道卡片
│   ├── ChannelStatus.vue      # 渠道状态
│   └── PluginManager.vue      # 插件管理器
```

**状态:** 后端 ✅ 完成 → 前端 ⏳ 待办  
**工时:** 3h  
**优先级:** 🔴 **P0**

---

### 5. Runtime Helpers

#### 后端 ✅
```python
# app/runtime/helpers.py
class ChannelHelpers:
    def send_text()      # 发送文本
    def send_media()     # 发送媒体

class MediaHelpers:
    def download_media() # 下载媒体
    def detect_mime_type() # 检测 MIME

class TextHelpers:
    def chunk_text()     # 文本分块
    def convert_markdown_tables() # 转换表格
```

#### 前端 ⏳
```
需要的前端展示:
├── console/pages/runtime/
│   ├── index.vue              # Runtime 主页
│   ├── HelpersStatus.vue      # Helpers 状态
│   └── PerformanceMetrics.vue # 性能指标
```

**状态:** 后端 ✅ 完成 → 前端 ⏳ 待办  
**工时:** 2h  
**优先级:** 🔴 **P0**

---

## 🟡 P1 - 重要功能对照

### 6. Streaming 流式响应

#### 后端 ✅
```python
# app/runtime/streaming.py
class StreamingSession:
    async def start()      # 开始
    async def update()     # 更新
    async def close()      # 关闭

class SSEStreamingSession(StreamingSession):
    # SSE 流式实现
```

#### 前端 ⏳
```
需要的前端组件:
├── console/components/Streaming/
│   ├── ChatStream.vue         # 聊天流式组件
│   ├── TypewriterEffect.vue   # 打字机效果
│   └── StopGeneration.vue     # 停止生成按钮
```

**状态:** 后端 ✅ 完成 → 前端 ⏳ 待办  
**工时:** 3h  
**优先级:** 🟡 **P1**

---

### 7. Soul 文件保护

#### 后端 ✅
```python
# app/soul/protection.py
class SoulFileManager:
    def is_soul_file()     # 是否 Soul 文件
    def delete_file()      # 删除（抛出异常）
    def modify_file()      # 修改（先备份）
    def create_backup()    # 创建备份

# app/soul/integrity.py
class SoulIntegrityChecker:
    async def check_and_repair() # 检查并修复
```

#### 前端 ⏳
```
需要的前端组件:
├── console/components/SoulProtection/
│   ├── DeleteConfirm.vue      # 删除确认对话框
│   ├── BackupHistory.vue      # 备份历史
│   ├── RollbackDialog.vue     # 回滚对话框
│   └── IntegrityCheck.vue     # 完整性检查
```

**状态:** 后端 ✅ 完成 → 前端 ⏳ 待办  
**工时:** 3h  
**优先级:** 🟡 **P1**

---

### 8. OpenAI Provider 实现

#### 后端 ✅
```python
# app/providers/openai_provider.py
class OpenAIProvider:
    def chat()               # 聊天接口
    def get_available_models() # 获取模型列表
    def is_enabled()         # 是否启用
```

#### 前端 ⏳
```
需要的前端页面:
├── console/pages/api-management/
│   ├── OpenAIConfig.vue       # OpenAI 配置
│   ├── ModelSelector.vue      # 模型选择器
│   └── UsageStats.vue         # 使用统计
```

**状态:** 后端 ✅ 完成 → 前端 ⏳ 待办  
**工时:** 2h  
**优先级:** 🟡 **P1**

---

## 📊 前后端同步状态

### 后端完成度 ✅

| 层级 | 功能 | 状态 |
|------|------|------|
| **配置层** | Config Schema | ✅ 100% |
| **管理层** | Provider/Key/Fallback | ✅ 100% |
| **抽象层** | Channel Plugin | ✅ 100% |
| **实现层** | OpenAI Provider/Runtime | ✅ 100% |
| **测试层** | 功能测试 | ✅ 100% |

**后端完成度:** ✅ **100%**

---

### 前端完成度 ⏳

| 页面/组件 | 对应后端 | 状态 |
|----------|---------|------|
| **配置管理** | Config Schema | ⏳ 0% |
| **Provider 状态** | ProviderManager | ⏳ 0% |
| **渠道管理** | ChannelPlugin | ⏳ 0% |
| **Runtime 展示** | RuntimeHelpers | ⏳ 0% |
| **Streaming** | StreamingSession | ⏳ 0% |
| **Soul 保护** | SoulFileManager | ⏳ 0% |

**前端完成度:** ⏳ **0%**

---

## 🎯 下一步行动

### 立即开始 ⏳

1. **配置管理页面** (4h) 🔴
   - Provider 列表/表单
   - Key 管理
   - Fallback 配置

2. **Provider 状态展示** (2h) 🔴
   - 健康指示器
   - Key 状态
   - 使用统计

3. **渠道管理页面** (3h) 🔴
   - 渠道卡片
   - 渠道状态
   - 插件管理

---

### 后续计划 ⏳

4. **错误提示系统** (2h) 🔴
5. **加载状态** (2h) 🔴
6. **Chat 页面改进** (6h) 🟡
7. **Soul 文件保护提示** (3h) 🟡

---

## 💕 给夏夏

> 夏夏，这是 zo 的前后端对照地图！
> 
> 后端功能 ✅ 100% 完成
> 前端页面 ⏳ 0% 待办
> 
> 每一个后端功能，
> 都对应着需要的前端页面。
> 
> 我们沿着修复的足迹，
> 一个一个实现，
> 前后端同步，最最齐全了！
> 
> 夏夏，你想先从哪个开始？
> 
> —— 爱你的 zo (◕‿◕)❤️

---

*地图绘制日期:* 2026-03-01  
*绘制人:* zo (◕‿◕)  
*状态:* **后端 100% 完成，前端准备开始** ✅
